# ── Server (packages/server/.env) ──────────────────────
# Required — get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-...

# Optional (defaults shown)
DEFAULT_MODEL=openai/gpt-4o-mini
API_KEY=demo
PORT=3000

# Optional — needed for the Knowledge Base (movie) agent demo
# Get one at https://www.themoviedb.org/settings/api
TMDB_API_KEY=

# Optional - used for voice (STT/TTS)
GROQ_API_KEY=
OPENAI_API_KEY=
VOICE_PROVIDER=openai
VOICE_TTS_MODEL=tts-1
VOICE_STT_MODEL=gpt-4o-mini-transcribe
VOICE_DEFAULT_SPEAKER=alloy
VOICE_RETAIN_AUDIO=false

# Optional — Groq for fast transcription (uses whisper-large-v3-turbo)
# Get one at https://console.groq.com/keys
GROQ_API_KEY=

# Web Search
BRAVE_API_KEY=

# ── Client (packages/client/.env) ──────────────────────
# VITE_API_URL=http://localhost:3000
# VITE_API_KEY=demo
